module.exports = {
    /* 一行字符数 超过会进行换行 默认 80 */
    printWidth: 200,
    /* tab 代表空格数 默认 2 */
    tabWidth: 4,
    /* 是否使用 tab 进行缩进 默认 false 表示用空格进行缩减 */
    useTabs: false,
    /* 字符串是否使用单引号 默认 false */
    singleQuote: true,
    /* 行位是否使用分号 默认 true */
    semi: false,
    /* 是否使用尾逗号 none | es5 | all */
    trailingComma: 'none',
    /* 大括号空格 默认为 true { foo: bar } */
    bracketSpacing: true
}
